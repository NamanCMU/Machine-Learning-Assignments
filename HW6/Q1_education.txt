For the education dataset, after going through the training set and visualizing it, it seems
that the variable "F" is most useful. In majority of the cases, the label assigned to grade is
same as the label which is assigned to the "F" attribute.